[pytest]
# Run from the python/ directory (conftest.py here puts the backend
# modules on sys.path and redirects on-disk state to a temp dir).
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
