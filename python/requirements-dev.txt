# Dev/test-only dependencies. NOT bundled into the PyInstaller backend
# (build-python.mjs installs requirements.txt only), so these never ship
# in the desktop app.
-r requirements.txt
pytest>=8.0
# Required: the order-recovery and US API adapter suites are async and use
# explicit @pytest.mark.asyncio marks. Without this plugin those marks are
# inert and ~29 tests fail rather than skip — silently, on any clean env.
pytest-asyncio>=1.0
